@model IEnumerable<FCMChurch.Models.StateName>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.State)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StateAbbreviation)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.State)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StateAbbreviation)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StateNameID }) |
            @Html.ActionLink("Details", "Details", new { id=item.StateNameID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StateNameID })
        </td>
    </tr>
}

</table>
